package uz.agroinvest.common.enums;

/**
 * How the project's assets are financed: fully by investors, by the farmer's
 * own already-held animals/assets (valued and verified at approval), or mixed.
 */
public enum FundingMode {
    INVESTOR_FUNDED,
    FARMER_ASSETS,
    MIXED
}
